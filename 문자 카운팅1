//문자열 s입력
//문자 c입력
//s만큼 반복하여 c개수 찾기
#include<iostream>
#include<string>
using namespace std;
 
int main() {
    string s;
    char c;
    getline(cin,s);
    cin>> c;
    int cnt = 0;
    for (int i = 0; i < s.length(); i++) {
        if (s[i] == c) cnt += 1;
    }
    cout << cnt;
}
