//n입력
//n만큼 정수 배열에 입력(arr[100])
//역순으로 출력(공백없이)
 
#include<iostream>
using namespace std;
 
int main()
{
    int n, arr[100];
    cin >> n;
    for (int i = 0; i < n; i++) cin >> arr[i];
     
    //출력
    for (int i = n - 1; i >= 0; i--) cout << arr[i];
}
